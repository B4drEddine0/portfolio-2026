// Add this import at the top of main.ts (line 2):
import { translations, currentLang, setLanguage, getLanguage, t, Language } from './translations'

// Replace line 4-11 (profile object) with:
const profile = {
  name: 'BadrEddine MassaAlKhayr',
  get title() { return t('title') },
  avatar: 'https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=300&h=300&fit=crop',
  email: 'badrdine03@gmail.com',
  phone: '+212687833857',
  location: 'Nador, Morocco',
  github: 'https://github.com/B4drEddine0',
  linkedin: 'https://www.linkedin.com/in/massaalkhayr/'
}

// Replace line 13 (about.bio) with:
const about = {
  get bio() { return t('bio') },
  services: [
    { icon: '💻', title: 'Développement Front-end', description: 'HTML, CSS, JavaScript, Tailwind CSS, Livewire, React JS - Création d\'interfaces modernes et responsive.' },
    { icon: '⚙️', title: 'Développement Back-end', description: 'C#, .NET, PHP, Laravel, JAVA, SpringBoot - Conception d\'APIs robustes et scalables.' },
    { icon: '🗄️', title: 'Bases de données & CMS', description: 'MySQL, PostgreSQL, WordPress - Gestion et optimisation de données avec expertise CMS.' },
    { icon: '🚀', title: 'Soft Skills & Méthodes Agile', description: 'Leadership d\'équipe, résolution de problèmes, Scrum, Kanban, planification de sprints.' }
  ]
}

// In init() function, add after line "const app = document.querySelector('#app')":
currentLang = getLanguage()

// In the sidebar HTML (around line 1100), add after social-links div and before download-cv-btn:
        <button class="lang-switcher" id="langSwitcher">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="12" cy="12" r="10"/>
            <path d="M2 12h20M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/>
          </svg>
          <span>${currentLang === 'en' ? 'FR' : 'EN'}</span>
        </button>

// Add event listener after line with "document.addEventListener('click', createRipple)":
  document.getElementById('langSwitcher')?.addEventListener('click', () => {
    const newLang: Language = currentLang === 'en' ? 'fr' : 'en'
    setLanguage(newLang)
    location.reload()
  })

// Update tab button texts (around line 1150-1170):
          <button class="tab-btn active" data-tab="about">
            <span>${t('about')}</span>
          </button>
          <button class="tab-btn" data-tab="resume">
            <span>${t('resume')}</span>
          </button>
          <button class="tab-btn" data-tab="skills">
            <span>${t('skills')}</span>
          </button>
          <button class="tab-btn" data-tab="portfolio">
            <span>${t('portfolio')}</span>
          </button>
          <button class="tab-btn" data-tab="contact">
            <span>${t('contact')}</span>
          </button>

// Update side drawer items similarly (around line 1120-1145)

// Update download CV button text:
          <span>${t('downloadCV')}</span>

// Update contact form placeholders and labels with t() function
// Update toast messages with t('messageSent') and t('messageError')
// Update filter tabs with t('all')
// Update content titles with t() functions
